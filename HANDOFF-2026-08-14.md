# Hand-off, 2026-08-14

Written at the owner's request at the end of a long session. **Nothing in this session's
review has been fixed.** Read this, then [REVIEW-2026-08-14.md](REVIEW-2026-08-14.md),
then `BUGS.md`'s header.

## Where the tree is

`main`, clean, pushed. **862 checks green.** 15 commits since `1f97fbf`.

**One release exists and one is deliberately withheld.** 1.12.0 is out (tag `v1.12.0`, DMG,
GitHub release), and its notes carry an appended known-issues section for R56/R57.
**1.13.0 was not cut, and that is a decision waiting on you** — the annotation transplant is
built, verified and merged, but two adversarial review rounds each found marks landing in the
wrong coordinate space, and the third round has not been run. It is off by default on `main`.
Releasing it is one command; the reason not to is the base rate.

## What changed in the app this session

Only two things touched `Sources/`:

1. **The annotation transplant** (`Sources/Annotations.swift`, ~1,100 lines) — TODO item 2,
   built, then twice reviewed and twice corrected. `BUGS.md` R58 has the whole story, and the
   corrections are more instructive than the feature: the rebuilt page is not always in the
   source's coordinate space; `/Rotate` and `/MediaBox` are inheritable and qpdf does not push
   them down; geometry arrays can be indirect; and a verification that compares the copy
   against the source agrees with itself.
2. **`publish`** — `BUGS.md` R59. Three defects: it could not replace a file on another volume
   (every re-run of a batch to an external drive failed with `EXDEV`), it deleted a *folder*
   aimed at it recursively while reporting success, and a cancel in the window before it could
   still publish.

Everything else was measurement, refusal, or documentation.

## The two decisions this session made, both refusals

- **TODO item 1 (`isPicture` after recognition) is refused for now.** The prize is real and
  now measured — **8.2 KB a page**, ~4.3 MB and 12% on the R49 book — but it would route more
  pages to 1-bit using `inkOutsideText`, whose recorded miss **is R56**. Blocked on the shape
  signal in `FEATURES.md`. `TODO.md` item 1 has the arithmetic; both of its previous figures
  were wrong.
- **A luminance signal for R56 is refused after four measured rounds.** The estimator is in
  `Tools/score-threshold-loss.swift` with a self-test, so round five starts from round four.
  It fails because the blind zone holds pale drawings you want kept, decorative shading you do
  not mind losing, and **show-through from the back of the sheet you actively want gone**.

## Fix these first, in this order

Full evidence for each is in `REVIEW-2026-08-14.md` under the given tag.

| # | what | where | why this order |
|---|---|---|---|
| 1 | **Retry Failed overwrites a file the batch protected** | A5.1 | content destruction, verified end to end, shipped |
| 2 | **A dropped file can trap the process uncatchably** | A7.1 | takes every concurrent file with it; default settings |
| 3 | **The run report contains the document's own text** | A4.1 | a file designed to be mailed; highest severity per line changed |
| 4 | A declined retry erases the record of what failed | A5.2 | |
| 5 | A second drop defeats U21's interlock | A5.3 | can put 8,001 rows over "Done — 1 of 1 succeeded" |
| 6 | **A1.2** before **A1.1** | A1.2, A1.1 | A1.1's only viable fix triples the sliver population A1.2 is about |
| 7 | The updater opens an unvalidated URL | A4.2 | needs control of the response, so not urgent |
| 8 | `preserveAnnotations` has no end-to-end test | A8.1 | the setting could be unwired and every annotation check would pass |

**A5.1's obvious fix does not work.** I tried it and reverted it; A5.1 records why, and it is
the defect restating itself — the protected path was an *input* of the earlier attempt, not
one of its outputs.

## Read this before touching the text layer

`REVIEW-2026-08-14.md` A6.1 is the most quietly expensive finding of the session: **three of
the four instruments CLAUDE.md invariant 3 tells you to re-measure with are compromised**, and
**the procedure is not executable as written** — two probes want a JSON shape nothing in
`Tools/` produces any more. `score-line-separation` divides PDFKit *lines* by Vision
*fragments* and can move opposite to the property it names.

So: **fix an instrument before trusting a text-layer measurement.** A6.3 has the
constant-versus-property tension map, measured across 233 documents, which did not exist
before and is what any future change to `headroomFactor`, `minimumVertical` or `reserveEms`
needs. Its headline: `headroomFactor` has **opposite optima on different material**, so no
single value is right for both dense newsprint and two-column magazine pages.

## What was not reviewed, and why

**Five of twelve review areas were never examined**: `Recogniser` + the helper protocol,
`Prefs`/views/`Updater`, `Runner`/`RunReport`, `Tools/`, and `Tests/`. Ten agent launches for
them failed silently — two batches of five, each leaving a 122-byte transcript and never
running. If you retry, **check that the transcript grows past a few kilobytes before assuming
an agent is working**; I did not, and lost an hour to five agents I believed were running.

The briefs are preserved verbatim in `REVIEW-2026-08-14.md` under "Not reviewed", because
writing them was most of the work. The most valuable is `Tests/`: nine checks that could not
fail have been found here by hand, and the tenth is unfound, with three concrete leads.

## Things I got wrong this session, so you can calibrate

Kept because this project's register already carries four entries that were wrong as written.

- **I said 1.12.0 was unreleased.** It had been out for hours. I read `TODO.md`'s prose
  instead of asking `gh`, and `git tag | tail` sorts *lexically*, so v1.10–v1.12 hide before
  v1.4.0. Ask the forge.
- **I reported a drift figure measured on the wrong file.** `pdf-extract-pages` uses PDFKit,
  which **normalises the media box to the origin** — so my extract had lost the exact geometry
  under test. Use `qpdf --pages` when geometry is the question.
- **My MRC memory figures were an artifact** (A3.1). `ru_maxrss` reads a multi-phase function
  as a *sum* of peaks because libmalloc keeps freed blocks mapped and dirty. Its verifier
  proved the phases do not overlap, to the byte.
- **I overstated A1.1's severity.** Seven of thirteen cases are cross-column pairs where the
  code is *right* to refuse, and my "PDFKit groups lines at ~8 pt" was the reviewer's own
  filter constant reported as a property of PDFKit.
- **I said "8 of 12 agents reported" when it was 7**, and did not check the five silent ones
  for over an hour.
- **I inverted the owner's stated order** — fixed R59 mid-sweep when the instruction was to
  finish the review first — and I treated "one agent at a time so the usage window doesn't get
  blown" as a budget cap on the *number* of agents when it was an instruction about
  serialisation.

The pattern in most of these is the one CONTRIBUTING §3 already names: **the instrument is
wrong more often than the code, and most convincing when it agrees with you.** Two of the
three findings I sent to adversarial verifiers came back corrected. Send the next one too.
