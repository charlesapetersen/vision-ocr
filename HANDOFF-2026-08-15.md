# Hand-off, 2026-08-15

An overnight run against `REVIEW-2026-08-14.md`'s fix order, at the owner's instruction to
work through it and cut a release at the end. **The release has not been cut** — see *What is
left* below, which is the honest reason.

Read this, then `BUGS.md`'s header, then `REVIEW-2026-08-14.md` for anything still open.

## Where the tree is

`main`, clean, pushed, **1031 checks green**. The suite also prints a **skip census** at the
end of every run now, so "all green" says how much of itself it exercised (T11).

Nothing is uncommitted and nothing is stranded in a worktree. That is deliberate: the previous
session's hand-off was written in the past tense about two steps that never ran, and its work
sat staged in `/private/tmp`. **Ask git, not this file.**

## What was fixed, in the order it landed

| finding | register | what it was |
|---|---|---|
| A5.1 | **R60** | Retry Failed published over a file the same batch protected — content destruction |
| A7.1, A7.3, A3.2 | **R61** | two declared-geometry conversions trapping uncatchably on the default route |
| A7.2 | **R62** | `inkCoverage` divided one population by another |
| — | **R63** | cancelling a Plain Text run reported every in-flight file as failed |
| A4.1 | **R64** | the run report carried up to 72 characters of the user's document |
| A5.2 | **R65** | a retry declined at the alert left the batch gutted, under "nothing was changed" |
| A5.3 | **R66** | the import interlock was a flag, so the first drop to land lowered it |
| A9.1 | **R67** | one `echo` in a dotfile cost JBIG2 for the whole session |
| A9.2 | **R68** | the report's JBIG2 row was the checkbox, not the route |
| A10.1 | **R69** | Extract Text could be made to silently OCR a picture of good text |
| A10.2 | **R70** | "PDF render DPI" is inert on the default route and said otherwise |
| A10.3 | **R71** | two presets are byte-identical and one claimed otherwise |
| A4.2 | **R72** | the updater opened whatever URL the response body named |
| A13.1 | **R73** | a NUL in *Languages* or *Custom words* aborted the whole app |
| A13.2 | **R74** | a document Vision read nothing from published as a silent success |
| A13.3 | **R75** | the manifest's newline guard missed every newline but one |
| A2.2 | **R76** | a cancelled text extraction still replaced the user's previous output |
| A9.3, A9.5, A9.6 | **R77** | `stop` gave up on an exited child, stranding the grandchild |
| A9.4, A9.7 | **R78** | four smaller reporting defects, one able to destroy a report |
| A3.3, A3.4, A4.3, A4.5, A4.6 | **R79** | a refusal naming the wrong remedy twice, and four more |
| A10.4, A10.5, A10.6, A5.4 | **R80** | the updater could offer a downgrade, and Check Now gave no way to take it |
| A12.1 | **T9** | the release gate could not see a destroyed page image *(previous session)* |
| A11.1–A11.4 | **T10** | invariant 2 had no working test *(previous session)* |
| A11.5, A11.6, A11.7, A8.3 | **T11** | the skip census was a number in a document, wrong by 57 |

## What is left, and why the release is not cut

**Four groups, and the first two gate the third.**

1. **The instruments — `A12.2`–`A12.8` and `A6.1`.** Not started. `A6.1` is the one that
   matters: three of the four instruments CLAUDE.md invariant 3 tells you to re-measure with are
   compromised, and **the procedure is not executable as written** — two probes want a JSON shape
   nothing in `Tools/` produces any more. `score-line-separation` can move *opposite* to the
   property it names. Until these are repaired, a text-layer measurement cannot be trusted.
2. **`A12.2`** in particular: three tools isolate pages with PDFKit, which changes the resolution
   production renders at, so **four `manifest.tsv` rows describe a pipeline nothing runs**.
3. **The text layer and the crop box — `A1.2`, then `A1.1`, then `C23`.** In that order: A1.1's
   only viable fix triples the sliver population A1.2 is about. **C23 is the one with user-visible
   harm still live** — every rebuild route publishes with no `/CropBox`, so the copy displays what
   the original hid, on **14 of 233 corpus documents and 577 of 16,987 pages**, worst case a third
   of the sheet. Its fix is a geometry change on the default route and needs the repaired probes
   plus a corpus run behind it; C13's entry records that the obvious check for it does not work,
   because re-OCRing the published copy can only ever report what is displayed.
4. **`A8.1`** — `preserveAnnotations` still has no end-to-end test, which is one of the owner's
   two stated preconditions for advertising that feature.

**So the release is not cut, and cutting it now would be the wrong call**: C23 changes what a
published page displays on 6% of the corpus, and the instruments that would tell you whether the
change is right are the ones under repair. Shipping the twenty-one fixes above *without* C23 is a
perfectly good release; shipping C23 unmeasured is not. That is a decision for the owner rather
than for whoever picks this up — the fixes are all on `main` and none of them is waiting on
anything.

## Things this session got wrong, so you can calibrate

Every one of these was caught by the project's own controls rather than by care.

- **`.rounded()` in R61's `sauvolaWindow`** would have moved the shipped Sauvola window by a
  pixel on about half of all pages — a threshold change on the default route, inside a fix for a
  trap. Caught by reviewing the staged diff before committing it.
- **A13.1's abort is placement-dependent.** A NUL raises only when something follows it; in the
  final position it silently *truncates* the value instead. The review recorded only the abort. A
  four-case probe against the real `Process.arguments` found the rest.
- **R73's first register text claimed the suite aborts *instead of* failing a check.** Its own
  mutant's saved output shows one FAIL line and *then* `exit=134`. Reading `mutation-out/`
  replaced the sentence.
- **The skip-census figures were hand-counted wrong** (16 and 13; measured 14 and 12), and the
  self-verifying assertion added in the same commit caught it before it landed. Static counting of
  `check(` lines over-counts, because some sit in branches that do not all execute.
- **A3.3's existing check asserted the opposite of the truth** — it was R26's belief written
  down. Verified A3.3's claim against `Recogniser.render` before inverting the check.
- **My own A5.3 door checks passed for the wrong reason** when they shared a model: `start()` got
  through, set `isRunning`, and `clearFiles` then refused on the guard it already had.
- **The A9.3 leak fixture has been manufacturing a stranded process on every run** and calling it
  a pass, because it asserted only `took < 5`.
- **`pgrep -f 'build/tests'` matches the waiter loops that contain the string**, including
  themselves. Four of them once waited on each other while nothing ran. Now in CLAUDE.md.

The pattern is the one CONTRIBUTING §3 already names, and it did not stop being true because
somebody was watching for it: **the instrument is wrong more often than the code, and most
convincing when it agrees with you.**
