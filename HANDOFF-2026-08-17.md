# Hand-off, 2026-08-17

The session that closed **R56 and R57** — the pale drawing erased and the tonal plate
blobbed — half of **C24**, and **R55** by decision. Read this, then `BUGS.md`'s header,
then `RESEARCH-shape-signals.md` if you are going anywhere near the picture detector.

## Where the tree is

**Ask git, not this file.** That sentence has been in seven consecutive hand-offs because
the prose went stale behind the work three times in three days.

Three commits on `main`: `2cf2370` (R56, R57), `12acd38` (C24's structural half, R55's
campaign), `41815b9` (a check that could not fail, and R55 closed).

`BUGS.md` is **one open**: `C24`, and only half of it. It does not destroy content and
there is no release blocker. **R55 is `WONTFIX`** — the campaign it asked for was run,
and the owner closed it on the arithmetic: the gate's over-exclusion costs the sweep
about 80 candidates and 0.7 GB against 1,164 and ~10 GB, the corpus half is solved by
adding upright-scanner documents by hand as `Why?` already was, and loosening the gate
would admit the hand-held photographs D1 exists to keep out.

**The 1.12.0 release notes carry a known-issues section naming R56 and R57 as live
defects.** Both are fixed on `main` and unreleased. Whoever cuts the next release should
strike that section rather than leave it describing a shipped binary's past.

## What landed

| register | what it was |
|---|---|
| **R57** | a continuous-tone plate over a fifth of a page routed to 1-bit and came out a black blob. Closed by measuring the page's *existing* tone constant **inside** the plate instead of over the whole sheet |
| **R56** | a pale line drawing erased rather than softened. Closed by a shape signal, on the sixth attempt and the second signal class |
| **C24**, half | a page that draws no XObject at all no longer takes another page's plate resolution — 85 pages, 3 documents, no threshold, 0 route changes |
| **R55** | `WONTFIX` on measurement plus arithmetic, with what would re-open it written down |

R56 and R57 are one idea: **the three old signals are statistics of the whole sheet, and
both defects are pages where that is the defect rather than the threshold.**

```
                          before        after
corpus pages moved            —      125 of 16,987   all 1-bit -> picture, none back
documents touched             —          21 of 233
gate over the 21 + 4 controls
  documents               25/25 ok    25/25 ok
  characters             4,639,609   4,641,234       +0.035%
  bytes                113,572,233  134,372,882      1.183x on a subset chosen
  controls                    —      4 of 4 byte-identical      to contain the change
suite                      1,101      1,127 checks
mutants                       —      12 new, all killed (2 after a check was repaired)
```

## The five things worth carrying forward

**1 · R57's caveat was wrong, and looking at three pages settled it in ten minutes.**
The entry said of its three band-dwelling corpus pages: *"Those three have not been
rendered and compared; that is the next step."* Rendered: `Ehrenreich_2000` p9 is a
Laurie Simmons photograph coming out as a black blob, and the two `HarpersMagazine-1938`
pages are dense type that 1-bit renders perfectly. One of each. **A step described in
the register as "the next step" and never taken was the whole question**, and it cost
ten minutes against the four measurement rounds spent around it. `Schwaller`'s 87
photograph pages — a much larger instance nobody had found — turned up in the same hour.

**2 · The thing that separates a drawing from show-through is not shape. It is place.**
Four luminance rounds were refused on the three-way blindness R56 records, and this
session refused *two more shape rounds* for the same reason: at any usable reduction,
real show-through merges into components taller than a line and emptier than a block, so
size and fill cannot tell it from a drawing. What works is that show-through lies **in**
the page's own type — it is the reverse page's type at the same measure — and a figure
sits where the type is not. `maximumInkUnderADrawing` is that question: holding
everything else fixed it takes `Doermann_1967` from 24 pages to 1 and `Ibson_2006` from
93 to 0, while the drawing fixture does not move by a thousandth — its plate has no type
under it at all. On the shipped constants both documents are at **0**.

**3 · An acceptance test written first did its job four times, and never the way it was
meant to.** `FEATURES.md` step 1 said to write it before the signal. What it caught: a
first draft that passed all six fixtures and moved **228 of `Himanen_2001`'s 255
pages**; a second that passed and moved 30 pages of `Boltanski_2006` — R38's document,
the one that inflated 9.45x when misrouted; a third whose ink-area gate silently dropped
a reversed-out advertisement that 1-bit destroys; and a fourth that fired on scanner-edge
blobs. None of the four is visible from the fixtures.
`Tools/score-routing-census.swift` is the instrument — all 16,987 pages, one row each,
and a diff names the ones that moved. It settled at **125 pages over 21 documents, every
one 1-bit → picture and none the other way**, with 87 of them the `Schwaller`
photographs. Read R57's corpus section: the bar said *no* page moves, and saying so
plainly is better than restating the bar afterwards.

**4 · The scoped read was worth more than the build.** `RESEARCH-shape-signals.md`
corrected the analysis resolution by 4x — 37.5 ppi is a *seed* in Leptonica, never a
decision surface, and `FEATURES.md` had specified the 40 DPI routing thumbnail — and
established the negative result that stopped a seventh round: **no published method
separates a pale drawing from show-through.** DjVuLibre contains no separator at all.
Read §7 and §6.1 before designing anything here.

**5 · Every number in a doc comment gets recomputed by the review, and three of mine
were wrong.** One table came from a superseded 75-cells-per-inch draft; one cell count
took the page inset off once instead of once at each end; and one justified a constant
by naming a page that constant does not in fact decide. All three were in comments
arguing that a constant was safe, which is the only kind of comment worth checking.

## What the review of this diff found, and it is the eleventh round running

Two agents, different lenses, and both found real defects in code written minutes
earlier — another round in which reviewing the previous round's work found real defects
in it, and this register has not yet had one that did not.

- **A trap I introduced.** `Int((dpi / markCellsPerInch).rounded())` on a DPI that
  descends from what the file declares: `MediaBox [0 0 1e-16 1e-16]` with a 2000 px
  image gives 1.44e21, which is 4 megapixels and passes every gate, and the conversion
  is an **uncatchable** crash that takes every concurrent file with it. This is A7.1 at
  a sixth site in one file, found by CONTRIBUTING 4b's own suggested grep.
- **A bound that did not bind.** `markCellCeiling`'s loop tested the *floored* product,
  so a 20,000,000 x 1 render floored the height to zero and allocated 8.75 million cells
  against a ceiling of four — in the constant whose own comment said it had been "caught
  before it shipped".
- **A page border read as a picture.** `largeMarkTone` gated on bounding-box area, so a
  3 px rule round the edge of a sheet qualified and the tone of everything it *encloses*
  was then measured — 1.54x the whole-sheet figure on a synthetic framed page. It takes
  two terms now, size *and* fill, and the paragraph below is why it is not just ink.
- **The census's own diff recipe was wrong.** Rows were streamed in completion order
  while the header claimed input order, so the `diff` the file prescribes reported
  **5,370 changed lines over 210 changed pages**. In the one instrument whose purpose is
  naming the pages that moved.
- **The fixture could not see the constant it was justifying.** `pale-drawing` sits on
  bare paper, so `maximumInkUnderADrawing` reads the same at every value including zero.
  `pale-chart` was added for that — a chart with its own axis numerals inside the plot
  frame, which is the ordinary layout of a figure in a book — and it immediately failed,
  because the statistic summed *ink* and a chart is thin. That is what moved the signal
  from a coverage sum to the extent of the largest mark.
- **Two published tables that did not reproduce**, both arguing that a constant was
  safe: one came from a superseded 75-cells-per-inch draft, one took the page inset off
  once instead of once at each end. The habit that catches these is recomputing every
  arithmetic claim in a doc comment from the code, and it is worth doing to your own. A
  third was found the same way afterwards, by me rather than by an agent — the
  justification for `largeMarkShare` named a page that the constant does not in fact
  decide, and the corpus said so in one query.
- **Two constants nothing could see, and the second is worse than the first.** All
  twelve new mutants were run, all twelve are killed, and two SURVIVED first. `const/minimumMarkContrast` had
  no fixture that could see it — its justification is 228 pages of `Himanen_2001`, and
  every fixture's marks were either 47 levels below the paper or absent; `faint-marks`,
  the same drawing at eleven levels, kills it. `const/minimumPlateFill` **had** a check
  written for it and the check **could not fail**: it drew its page frame 40 px from the
  edge while the analysis crops the outer sixteenth, 75 px on that page, so the frame
  never entered the mask. Moved to 150 px it bites — the frame's own tone reads 0.1668
  against the sheet's 0.1223. Writing the check is not the same as watching it fail, and
  `mutate.py` is what tells you which one you did.

**And two things the reviews did not catch, which the corpus did.** Gating the tone
question on a component's own *ink* — the first fix for the page-frame finding — silently
dropped `Scott_TK` p13, a reversed-out advertisement that 1-bit really does destroy; and
the size gate that removes three `Doermann_1967` scanner-edge false positives removes
that advertisement with them. Both were settled by rendering the pages and choosing the
direction that keeps content. **A review finding's first fix is not automatically
right**, which is the same lesson as C23's refusal in the last hand-off.

## What is left

- **`C24`, half fixed.** A page that draws no XObject at all now has no image, whatever
  its `/Resources` can reach — 85 corpus pages over 3 documents, structural, no
  threshold, and **0 route changes**. Those documents come out **7.4% smaller and
  recover 0.63% fewer characters**, because 300 DPI rasterises vector text slightly less
  well than the 370 they were accidentally getting. That is an argument about
  `fallbackRebuildDPI` and the entry says so. **The other 45 pages** draw a *different*
  image than the shared dictionary holds — **39 smaller and 6 wider**. (This sentence said
  plainly "smaller" until 2026-08-17. It is the same wrong word the entry itself had already
  corrected: both walks pick by *area* and report *width*, so 6 of the 45 are wider.)
  This paragraph said that half "still wants the
  threshold the entry refused"; **measured later the same day, it does not.** The three
  pages `minimumScanPixelWidth` actually faces were rendered both ways by
  `Tools/score-rebuild-dpi.swift` and it is right about all three — `Batzell` p22 retains
  92.8% of its own 291 words at its own 70.6 DPI against 94.2% at 369.6, for 87% fewer
  bytes — so what is left of this half is the **wiring**, behind a corpus gate run.
  `score-routing` still refuses `Batzell` and `AI 2027` because its four-page sample lands
  in that half.
- **`R55` is closed — `WONTFIX`, 2026-08-17, on the owner's call.** The campaign: `Tools/score-illumination.swift`
  measures what the entry asked for — the *consistency* of the lighting across a
  document's pages. **No mechanical scan in 204 exceeds 0.0373**, and `Why?` reads
  0.0196, so this rules a flatbed out and rescues the document that opened the entry.
  It does not rule hand-held *in*: two documents from the extremes of the survey's
  "Random Photograph" set were rendered and **both are scans** — a flat ProQuest page at
  the low end and a full-bleed magazine advertisement at the high end, whose score is
  high because its *content* varies rather than its lighting. The gate is unchanged, and
  the entry now names the two things needed first: a lighting measure that is not
  content-dependent, and a hand-held set somebody has looked at.
- **`A13.4`'s remainder**, three small things in `Runner`/`RunReport`, unchanged:
  `recognitionInHelpers` is a fresh probe rather than a record of what happened; the
  progress bar goes *backwards* after a helper falls back; and `helperStallSeconds = 900`
  is fifteen minutes per file before a wedged helper is given up on.
- **`A1.4`**, unchanged: two welds in 1,684 adjacent pairs where the reserve *did* apply.
  Countable now, via `welded=` in `score-line-separation`.
- **The library sweep's step 1 wants re-running before step 2 acts on it**, unchanged,
  about 50 minutes. Note R55's closure does **not** change what the sweep may touch —
  the gate is untouched.
- **Three things R56's fix was said to unblock and does not, yet.** R35's per-page
  background factor, R49's paper detector and `TODO.md` item 1 were each refused for want
  of a shape signal. There is one now, but it answers a narrower question than any of
  them asks — it finds a pale mark bigger than about 2.2 inches on a side, and a large
  mark with tone in it. Re-open them on measurement, not on the assumption that the
  blocker has gone.

## If you are picking this up cold

The order that worked, and it is `FEATURES.md`'s: re-read the refusals sceptically,
write the acceptance test *first*, check what is already in the tree, and only then read
one external source and build. Two of those four steps were wrong in their content and
the order still saved the work — step 0 found that R57's caveat was hiding a real
photograph, and step 1 caught three signals that passed every fixture and wrecked the
corpus.

The instruments to know about: `Tools/score-routing-census.swift` (every page, one row,
diff names what moved), `Tools/make-plate-fixtures.swift` (eight adversarial pages,
built by `run_tests.sh` and handed to the suite so they cannot drift), and
`Tools/score-illumination.swift` (R55's, kept though the entry is closed).

## Environment, unchanged and still true

- **Never run two suites at once, in any two worktrees** — `build/tests` has no bundle
  id, so every worktree shares `~/Library/Preferences/tests.plist`. Check `pgrep -x tests`
  first, and do not edit `Sources/` while `mutate.py` is running.
- **macOS bash is 3.2.57**: `"${ARRAY[@]}"` under `set -u` on an *empty* array is fatal.
- Tool build line: copy the tool to `<scratch>/main.swift`, then `swiftc -O -o out -target
  "$(uname -m)-apple-macos13.0" <every Sources/*.swift except App.swift> <scratch>/main.swift`.
  Or `Tools/check-tools-compile.sh <name>`.
- `testdocs/` filenames contain spaces. Glob, quote, never retype.
